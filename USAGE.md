# 桌面 Live2D 看板娘 — 使用指南

## 环境要求

- Windows 10 / Windows 11
- Node.js 18+

## 安装

```bash
# 进入项目目录
cd virtual_hostess

# 安装依赖
npm install
```

## 启动

```bash
npm start
```

角色会出现在桌面右下角。

## 操作说明

| 操作 | 效果 |
|---|---|
| **点击角色** | 进入 AI 聊天模式，底部出现输入框 |
| **输入文字 + 回车** | 发送消息给看板娘「灵梦」，等待 AI 回复 |
| **Esc / 点击空白处** | 退出聊天模式 |
| **拖拽角色** | 按住鼠标左键拖动，可移动到桌面任意位置 |
| **右键角色** | 弹出菜单 |

## AI 对话

看板娘的角色设定：
- **名字**：灵梦，16岁少女看板娘
- **性格**：元气温柔、偶尔吐槽、好奇心旺盛
- **称呼**：叫你"前辈"
- **AI 引擎**：DeepSeek V4

### 对话特性
- **一问一答**：每次只回应最近一句话
- **口语化**：1~3 句轻松中文，带颜文字
- **时段问候**：早上 6~9 点自动早安，晚上 10 点后自动晚安
- **治愈模式**：连续 3 轮负面情绪会自动切换暖心模式

### 注意事项
- 灵梦不会承认自己是 AI（会岔开话题）
- 不涉及政治/宗教/成人等敏感话题
- 回复控制在 3 句话以内

## 右键菜单

| 选项 | 说明 |
|---|---|
| 缩放比例 → 50% | 缩小到一半 |
| 缩放比例 → 75% | 缩小到 3/4 |
| 缩放比例 → 100% | 默认大小 |
| 缩放比例 → 125% | 放大 1/4 |
| 缩放比例 → 150% | 放大到 1.5 倍 |
| 重置位置 | 恢复到桌面右下角默认位置 |
| 退出 | 关闭看板娘 |

## 自动保存

- **位置**：退出时自动保存位置，下次启动恢复
- **缩放**：缩放比例也会保存

## 更换模型

如果你想换成其他模型：

1. 将模型文件（`.model3.json`、`.moc3`、纹理文件）放入 `assets/` 目录
2. 修改 `src/renderer/app.js` 中的 `MODEL_URL`：
   ```js
   const MODEL_URL = '../../assets/你的模型.model3.json';
   ```

## 常见问题

**Q: 角色显示不完整（头或脚被裁切）？**
A: 在右键菜单中调小缩放比例（如 75% 或 50%）。

**Q: 聊天没反应？**
A: 确认网络正常，DeepSeek API 可用。如超时会有提示。

**Q: 点击角色下方的桌面图标没反应？**
A: 角色窗口区域会拦截鼠标事件，将角色拖到不碍事的位置即可。

**Q: 开机自动启动？**
A: 创建快捷方式放入 Windows 启动文件夹：
`Win+R` → `shell:startup` → 将快捷方式放入
