# 桌面 Live2D 看板娘 — 使用指南

## 环境要求

- Windows 10 / Windows 11
- Node.js 18+

## 安装

```bash
# 进入项目目录
cd virtual_hostess

# 安装依赖
npm install
```

## 启动

```bash
npm start
```

角色会出现在桌面右下角。

## 操作说明

| 操作 | 效果 |
|---|---|
| **拖拽角色** | 按住鼠标左键拖动，可移动到桌面任意位置 |
| **点击角色** | 角色做出反应动作（弹跳/晃动），弹出对话气泡 |
| **右键角色** | 弹出菜单 |

## 右键菜单

| 选项 | 说明 |
|---|---|
| 缩放比例 → 50% | 缩小到一半 |
| 缩放比例 → 75% | 缩小到 3/4 |
| 缩放比例 → 100% | 默认大小 |
| 缩放比例 → 125% | 放大 1/4 |
| 缩放比例 → 150% | 放大到 1.5 倍 |
| 重置位置 | 恢复到桌面右下角默认位置 |
| 退出 | 关闭看板娘 |

## 自动保存

- **位置**：退出时会自动保存角色在桌面上的位置，下次启动恢复
- **缩放**：缩放比例也会保存

## 关于模型

默认使用 Live2D 官方免费素材「Haru」（春），这是一个 Cubism 4 模型。

### 更换模型

如果你想换成其他模型：

1. 将模型文件（`.model3.json`、`.moc3`、纹理文件）放入 `assets/` 目录
2. 修改 `src/renderer/app.js` 中的 `MODEL_URL` 为本地路径：
   ```js
   const MODEL_URL = '../../assets/你的模型.model3.json';
   ```

免费 Live2D 模型可以在以下渠道获取：
- [Live2D 官方免费素材](https://www.live2d.com/download/sample-data/)
- GitHub 搜索 `live2d model collection`

## 常见问题

**Q: 角色显示不完整（头或脚被裁切）？**
A: 尝试在右键菜单中调小缩放比例（如 75% 或 50%）。

**Q: 角色无法拖动？**
A: 确保点击的是角色身体（不是透明区域）。按住左键拖动即可。

**Q: 点击角色下方的桌面图标没反应？**
A: 角色的窗口区域（约 600×850 像素）会拦截鼠标事件。将角色拖到不影响使用的位置即可。

**Q: 开机自动启动？**
A: 可以创建快捷方式放入 Windows 启动文件夹：
`Win+R` → `shell:startup` → 将 `npm start` 的快捷方式放入
